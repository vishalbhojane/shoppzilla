import { useContext } from "react";
import ShopContext from "../context/ShopContext";

const Pagination = () => {
    const { pagedData } = useContext(ShopContext)

    return (
        <>
        
        </>
    );
}

export default Pagination;